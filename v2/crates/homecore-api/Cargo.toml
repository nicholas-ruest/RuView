[package]
name = "homecore-api"
version = "0.1.0-alpha.0"
edition = "2021"
license = "MIT"
authors = ["rUv <ruv@ruv.net>", "HOMECORE Contributors"]
description = "Wire-compatible Axum REST + WebSocket port of Home Assistant's API (ADR-130)"
repository = "https://github.com/ruvnet/RuView"

[lib]
name = "homecore_api"
path = "src/lib.rs"

[features]
default = []
# ADR-172 §2.1 degradation contract — forward to the ecosystems crate's
# live-path gates. OFF by default: Apple pairing / Matter commissioning
# return the documented 409s until the HAP server / rs-matter SDK land.
apple-live = ["homecore-ecosystems/apple-live"]
matter-live = ["homecore-ecosystems/matter-live"]

[[bin]]
name = "homecore-api-server"
path = "src/bin/server.rs"

[dependencies]
homecore = { path = "../homecore", version = "0.1.0-alpha.0" }
# ADR-172 ECO-FABRIC — pure-domain ecosystems manager (no axum dep; we wire it).
homecore-ecosystems = { path = "../homecore-ecosystems" }

axum = { version = "0.7", features = ["ws", "json", "macros"] }
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace"] }

serde = { version = "1", features = ["derive"] }
serde_json = "1"

thiserror = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
chrono = { version = "0.4", features = ["serde"] }

uuid = { version = "1", features = ["v4", "serde"] }
dashmap = "6"
futures-util = { version = "0.3", default-features = false, features = ["sink"] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
hyper = "1"
http-body-util = "0.1"
# End-to-end WS handshake + reply tests (HC-WS-01/02, ADR-161).
tokio-tungstenite = "0.24"
futures-util = { version = "0.3", default-features = false }
